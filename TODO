- add a kind of setup that ensures the health in startup (it must run after logs, before run_server, this feature must be a setting in the config file)

- keys be autogenerated at tls module (they must be trusted, key and certs paths get to be optional)

- server management while running, also run as a service,

- make docs

- in config for each route: set if its a server(also webtransport*to implement* or default) or proxy, how to handle each http request, if itll use caching*to implement*

- caching, webtransport, enhance datagrams.
